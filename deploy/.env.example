# Configuración para Deployment en EC2
# Copia este archivo a .env.deploy y configura tus valores

# Información de la instancia EC2
EC2_HOST="12.34.56.789"                    # IP pública de tu instancia EC2
EC2_USER="ubuntu"                          # Usuario SSH (ubuntu para Ubuntu, ec2-user para Amazon Linux)
KEY_PATH="/home/tu-usuario/.ssh/tu-key.pem" # Ruta completa a tu archivo .pem

# Configuración opcional
DEPLOY_PATH="/var/www/html"                # Directorio donde se despliega la aplicación
DOMAIN=""                                  # Tu dominio (opcional, para SSL)

# Configuración de AWS Cognito (para producción)
# Actualiza src/environments/environment.prod.ts con estos valores
COGNITO_USER_POOL_ID="us-east-1_XXXXXXXXX"
COGNITO_CLIENT_ID="xxxxxxxxxxxxxxxxxxxxxxxxxx"
COGNITO_REGION="us-east-1"

# Notas:
# 1. Este archivo contiene información sensible, NO lo subas a git
# 2. Asegúrate de que tu archivo .pem tenga permisos 400: chmod 400 ~/.ssh/tu-key.pem
# 3. Verifica que tu Security Group de EC2 permita tráfico HTTP (puerto 80) desde anywhere (0.0.0.0/0)
# 4. Si usas un dominio personalizado, apúntalo a la IP de tu EC2
